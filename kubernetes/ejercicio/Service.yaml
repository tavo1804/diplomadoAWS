apiVersion: v1
kind: Service
metadata:
  name: servicio-gustavo-velasquez
  namespace: namespace-gustavo-velasquez
spec:
  type: LoadBalancer
  selector:
    app: httpd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80